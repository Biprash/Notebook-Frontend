import React, { ReactElement } from 'react'

interface Props {
    
}

function Login({}: Props): ReactElement {
    return (
        <div>
            <h1>Login</h1>
        </div>
    )
}

export default Login
